import {
  default as React,
} from "react";

import {
  configureStore,
} from "./redux";

import {
  default as Root,
} from "./components/Root"

export { configureStore };

export const RootComponent = Root;
